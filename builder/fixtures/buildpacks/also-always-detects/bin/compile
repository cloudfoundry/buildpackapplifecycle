#!/bin/bash
# vim: set ft=sh

BUILD_DIR=$1
CACHE_DIR=$2
DEPS_DIR=$4

echo WOO
echo also-always-detects-buildpack > $BUILD_DIR/compiled
echo also-always-detects-buildpack > $CACHE_DIR/compiled
echo also-always-detects-buildpack > $DEPS_DIR/compiled

